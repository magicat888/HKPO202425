# Add README.md to the staging area  
git add .

# Commit the changes with a message  
git commit -m "new commit - add files to the repo"  

# Push the changes to the remote repository  
git push -u origin main